# 前端環境變數範例。
#
# 本機開發：不需要 .env，VITE_API_URL 留空即可，
#   Vite dev server 會把 /api 代理到本機後端（見 vite.config.ts）。
#
# 部署到 Vercel：在 Vercel 專案的 Environment Variables 設定下列變數，
#   值為 Render 後端網址，結尾「不要」加斜線。
VITE_API_URL=https://your-api-name.onrender.com
